// Main theme configuration.
// Why: Provides the central theme setup used by the application.
import 'package:flutter/material.dart';

import 'app_colors.dart';

class AppTheme {
  static ThemeData light = ThemeData(colorSchemeSeed: AppColors.primary);
}
