// Application-level configuration.
// Why: Store app-wide configuration such as environment settings and feature flags.
class AppConfig {
  static const appName = 'Lalbaba Online';
  static const version = '1.0.0+1';
}
