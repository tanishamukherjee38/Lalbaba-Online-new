// API-related constants.
// Why: Keep base URLs and shared API constants in one centralized location.
class ApiEndpoints {
  static const login = '/login';
  static const profile = '/profile';
  static const products = '/products';
}
